Drop your empty-state illustrations here (e.g. `no-friends.png`, `no-groups.png`,
`no-expenses.png`, `no-results.png`) and swap the `icon` prop in
`src/components/EmptyState.jsx` for an `<img src={...} />` once ready.
